{
  "input_datatypes": [
    {
      "name": "cam_import_by_id",
      "label": "CAM VM import by ID"
    }
  ],
  "input_groups": [
    {
      "name": "cloud",
      "label": "CAM Import Variables"
    }
  ],
  "template_input_params": [
    {
      "name": "cam_hostname",
      "type": "string",
      "description": "CAM host name or IP",
      "label": "CAM host name or IP",
      "required": true,
      "secured": false,
      "default": "${cam_import_by_id.cam_hostname}"
    },

    {
      "name": "cam_username",
      "type": "string",
      "description": "CAM user name",
      "label": "CAM username",
      "required": true,
      "secured": false,
      "default": "${cam_import_by_id.cam_username}"
    },
    {
      "name": "cam_password",
      "type": "string",
      "description": "CAM password",
      "label": "CAM password",
      "required": true,
      "secured": true,
      "default": "${cam_import_by_id.cam_password}"
    },
    {
      "name": "cam_cloudconnection_id",
      "type": "string",
      "description": "CAM ID of the cloud connection used to connect to the cloud where the instance is going to be imported from",
      "label": "CAM Cloud Connection ID",
      "required": true,
      "secured": false,
      "immutable": false,
      "default": "${cam_import_by_id.cam_cloudconnection_id}"
    },
    {
      "name": "cam_template_id",
      "type": "string",
      "description": "CAM ID of the import template used as a base for terraform import",
      "label": "CAM Import Template ID",
      "required": true,
      "secured": false,
      "default": "${cam_import_by_id.cam_template_id}"
    },
    {
      "name": "cam_instancename",
      "type": "string",
      "description": "Name of the CAM instance that will be created after the import",
      "label": "CAM Instance Name",
      "required": true,
      "secured": false
    },
    {
      "name": "cam_namespace",
      "type": "string",
      "description": "CAM namespace into which the imported instance will be created",
      "label": "CAM namespace",
      "required": true,
      "secured": false,
      "default": "default"
    },
    {
      "name": "cloud_instance_id",
      "type": "string",
      "description": "Cloud native ID of the instance to be imprted. e.g. 338aefb7-6987-4c43-88d5-09351b549b7f",
      "label": "Cloud Native Instance ID",
      "required": true,
      "secured": false
    }
  ],
  "template_output_params": [
    {
      "name": "cam_instance_url",
      "label": "CAM Imported Instance URL",
      "description": "CAM Imported Instance URL",
      "type": "url",
      "secured": false
    }
  ]
}
